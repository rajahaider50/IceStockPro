// IceStock Pro starts completely empty — no default categories or items.
// The shop owner adds everything from scratch via the Add / Manage buttons.
export async function seedIfEmpty() {
  // Intentionally does nothing: the app ships with a blank catalog.
  // Categories and stock items are created only by the user.
  return;
}
